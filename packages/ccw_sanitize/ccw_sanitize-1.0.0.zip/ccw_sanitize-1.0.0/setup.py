from distutils.core import setup
#from astroid.__pkginfo__ import author, author_email, description

setup(
      name = 'ccw_sanitize',
      version = '1.0.0',
      py_modules = ['ccw_sanitize'],
      author = 'ccw',
      author_email = 'ccw3435@naver.com',
      description = 'a simple printer of sanitize'
      )
