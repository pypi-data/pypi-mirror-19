[annotation]
assembly = option('hg18', 'hg19')

[oncodrivefm]
significance_threshold = float
samples_threshold = integer
genes_filter_file = file(default=None)

[oncodriveclust]
significance_threshold = float
samples_threshold = integer
genes_filter_file = file(default=None)

[mutsig]
significance_threshold = float
genes_filter_file = file(default=None)

[__many__]

    [[oncodrivefm]]
    significance_threshold = float(default=None)
    samples_threshold = integer(default=None)
    genes_filter_file = file(default=None)

    [[oncodriveclust]]
    significance_threshold = float(default=None)
    samples_threshold = integer(default=None)
    genes_filter_file = file(default=None)

    [[mutsig]]
    significance_threshold = float(default=None)
    genes_filter_file = file(default=None)

