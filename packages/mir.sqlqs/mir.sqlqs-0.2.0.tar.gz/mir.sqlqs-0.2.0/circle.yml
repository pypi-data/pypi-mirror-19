machine:
  python:
    version: 3.5.2
dependencies:
  override:
    - sudo apt-get update; sudo apt-get install python-apsw
    - pip3 install -r requirements.txt
    - pip3 install -e .
test:
  override:
    - pytest
    - bash <(curl -s https://codecov.io/bash)
