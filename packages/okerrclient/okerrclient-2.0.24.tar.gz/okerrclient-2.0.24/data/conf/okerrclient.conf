#
# install:
#
# you need to change textid and keypass
# you may need also to set secret (secret = mypassword) and keyuser 
# 
# cp /usr/local/okerrclient/okerrclient.conf /etc/
#
#

textid=YourProjectTextID
secret
keyuser = client
keypass = YourKeysPassword
sequence = [ MKSEQ servers:$_name|conf:anyserver ]
tpconf = [ RUN:safebin=dig ]

