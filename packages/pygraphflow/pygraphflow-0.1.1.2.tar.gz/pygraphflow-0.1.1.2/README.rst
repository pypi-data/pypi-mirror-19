pygraphflow
-----------

A python driver for GraphFlow allowing to build GraphFlowClient objects.
Also used to build a Query using the QueryBuilder and pass it to the client's method execute_query()

