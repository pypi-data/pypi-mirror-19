from setuptools import setup, find_packages

setup(name='undict',
      version='0.1.0',
      packages = ['undict'],
      # find_packages(),
      author='firstprayer',
      author_email='zhangty10@gmail.com',
      description='Undict - Easier API for using Dictionary, especially with json',
      url='https://github.com/firstprayer/undict.git',
      install_requires=[],
)
